// Portfolio is ready.
// Smooth navigation is handled by CSS.
// This script keeps the page ready for future interactive features.

document.addEventListener("DOMContentLoaded", function () {
    console.log("Gopika Web Studio portfolio loaded successfully.");
});